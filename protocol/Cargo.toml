[package]
name = "protocol"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

# no_std 协议库，被 RP2040(ARM) 与 ESP32(Xtensa) 两个工具链共用
[dependencies]
serde = { version = "1.0", default-features = false, features = ["derive"] }
postcard = { version = "1.1", default-features = false }

[lib]
